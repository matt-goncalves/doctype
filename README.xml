<document>

<head>
  <title>My Custom Document Definition and Handlers</title>
  <meta name="author" content="Adam Prescott" />
  <meta name="date" content="2025-06-27T14:48:18-0300" />
  <meta name="keywords" content="readme file, doctype, document handling,
                                 organization, standards" />
  <meta name="version" content="1.0" />
</head>

<body>

<par>
  This directory holds the stylesheets and handlers for my new document format. It's
  part of my new standard, and will be used extensively. Keep the directory and
  its contents well guarded!
</par>

<section>

<title>Rules</title>

<list type="enum">

  <item>All documents are XML from now on</item>

  <item>The <![CDATA[<?xml version="1.0" encoding="utf-8"?>]]> header is
  omitted</item>

  <item>All texts are hard-wrapped at 80ch</item>

  <item>Generic elements will be used as much as possible, especially in
  metadata fields</item>

  <item>XML indentation will be as human-readable as possible</item>
  
  <item>Comments will be used to organize the text</item>

  <item>Modularization will be done with <![CDATA[&entity;]]></item>

  <item>Inline Unicode symbols like dash, smart-quotes, etc. will be handled
  with a post-processor---they won't be inserted into the HTML, not even through
  entities, for readability</item>

  <item>Documents will be divided between header (metadata) and body
  (content)</item>

  <item>All document manipulation will be done with XSLT (except typographic
  post-processing to replace quotes and such)</item>

  <item>Whatever can be done with <code>xsltproc</code> and
  <code>libxml2-utils</code> will be done, so that I won't need any extra
  tools</item>

  <item>Documents will not have hard-coded DOCTYPE declaration---this will be
  used as parameter when processing the document itself</item>

</list><!-- close enum list -->

</section><!-- close rules -->

</body>
</document>
